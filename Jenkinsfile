node{
                def rev = checkout(scm).GIT_COMMIT
                def  branch = checkout(scm).GIT_BRANCH
                sh 'echo ${rev} ${branch}'
                sh 'printenv'
                
        }
